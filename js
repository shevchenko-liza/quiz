/*const button = document.querySelector("#button");
const ex = document.querySelector("#ex");

button.addEventListener("click", function() {
    console.log("Click");
    if (ex.classList.toggle('title')) {
        button.textContent = "Open"
    } else {
        button.textContent = "close"
    }
});
*/

console.log("hello")